services:
  check-qb:
    image: unraiders/check-qb
    container_name: check-qb
    restart: unless-stopped
    volumes:
      - ./config:/app/config
    environment:
      - TZ=Europe/Madrid
    env_file:
      - .env
